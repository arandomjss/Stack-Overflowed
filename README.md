# Stacko Hackathon

Starter repository for the hackathon. Contains minimal scaffolding for quick project bootstrapping.

Getting started

- Run the Python starter: `python src/main.py`
- Run the Node starter: `node src/index.js`

Choose a language and add dependencies in `requirements.txt` or `package.json`.
